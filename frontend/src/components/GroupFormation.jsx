import { Users, X } from 'lucide-react';

export default function GroupFormation({ group, activeProject, groupMode, setGroupMode, groupForm, setGroupForm, submitGroup, groupSaving }) {
  if (group) return <GroupSummary group={group} activeProject={activeProject} />;
  return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8">
    <div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-blue-50 text-blue-600"><Users className="h-5 w-5" /></span><div><h2 className="text-xl font-black">My Group</h2><p className="mt-1 text-sm text-slate-500">Form an interdisciplinary team before selecting an AI project.</p></div></div>
    {!groupMode ? <div className="mt-8 grid gap-4 sm:grid-cols-2"><button onClick={() => setGroupMode('create')} className="rounded-2xl border border-blue-200 bg-blue-50 p-6 text-left hover:border-blue-500"><p className="text-lg font-black text-blue-900">Create Group</p><p className="mt-2 text-sm text-blue-700">Become the group leader and invite teammates with a unique join code.</p></button><button onClick={() => setGroupMode('join')} className="rounded-2xl border border-slate-200 bg-slate-50 p-6 text-left hover:border-blue-300"><p className="text-lg font-black text-slate-900">Join Existing Group</p><p className="mt-2 text-sm text-slate-600">Use a Group ID or join code from your group leader.</p></button></div> : <form onSubmit={submitGroup} className="mt-8 space-y-4"><div className="flex items-center justify-between"><h3 className="font-black">{groupMode === 'create' ? 'Create your group' : 'Join a group'}</h3><button type="button" onClick={() => setGroupMode(null)}><X className="h-5 w-5 text-slate-400" /></button></div>{groupMode === 'create' ? <><input required value={groupForm.name} onChange={(event) => setGroupForm({ ...groupForm, name: event.target.value })} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm" placeholder="Group name" /><label className="block text-sm font-bold text-slate-600">Maximum team size<select value={groupForm.maxSize} onChange={(event) => setGroupForm({ ...groupForm, maxSize: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 text-sm"><option value="2">2 members</option><option value="3">3 members</option><option value="4">4 members</option><option value="5">5 members</option><option value="6">6 members</option></select></label><input value={groupForm.allowedAcademicYears.join(', ')} onChange={(event) => setGroupForm({ ...groupForm, allowedAcademicYears: event.target.value.split(',').map((item) => item.trim()).filter(Boolean) })} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm" placeholder="Allowed academic years (optional), e.g. 2026, 2027" /><p className="text-xs text-slate-500">Leave academic years empty to allow eligible students from any batch.</p></> : <><input value={groupForm.joinCode} onChange={(event) => setGroupForm({ ...groupForm, joinCode: event.target.value.toUpperCase() })} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm uppercase" placeholder="Join code" /><input value={groupForm.groupId} onChange={(event) => setGroupForm({ ...groupForm, groupId: event.target.value })} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm" placeholder="Group ID (optional)" /><p className="text-xs text-slate-500">You cannot join another group, a full group, or a finalized group.</p></>}<button disabled={groupSaving} className="w-full rounded-xl bg-blue-600 px-4 py-3 text-sm font-black text-white disabled:opacity-50">{groupSaving ? 'Processing...' : groupMode === 'create' ? 'Create Group' : 'Join Group'}</button></form>}
  </section>;
}

function GroupSummary({ group, activeProject }) {
  return <div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-blue-600">My group</p><h2 className="mt-2 text-xl font-black">{group.name}</h2></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-black text-emerald-700">{group.members?.length || 0}/{group.maxSize} members</span></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-blue-50 p-4"><p className="text-xs font-bold text-blue-600">Group ID</p><p className="mt-1 break-all text-sm font-black text-blue-950">{group.groupId || 'Generating ID...'}</p></div><div className="rounded-xl bg-amber-50 p-4"><p className="text-xs font-bold text-amber-700">Join code</p><p className="mt-1 text-lg font-black tracking-wider text-amber-950">{group.joinCode || 'Generating code...'}</p></div></div><div className="mt-5 space-y-3">{(group.members || []).map((member) => <div key={member._id} className="flex items-center justify-between rounded-xl bg-slate-50 p-4"><div><p className="font-bold">{member.name}</p><p className="text-xs text-slate-500">{member.email} · {member.department}</p></div>{String(group.leader?._id || group.leader) === String(member._id) && <span className="rounded-full bg-blue-100 px-2.5 py-1 text-xs font-black text-blue-700">Leader</span>}</div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-black">Project setup</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-slate-400">Selected domain</dt><dd className="mt-1 font-black">{group.domain?.join(', ') || activeProject?.domain || 'Not selected'}</dd></div><div><dt className="text-slate-400">Difficulty</dt><dd className="mt-1 font-black">{group.difficultyLevel}</dd></div><div><dt className="text-slate-400">Technologies</dt><dd className="mt-1 font-black">{group.preferredTech?.join(', ') || 'Not selected'}</dd></div><div><dt className="text-slate-400">Status</dt><dd className="mt-1 font-black">{group.finalized ? 'Finalized' : group.status}</dd></div></dl></section></div>;
}


